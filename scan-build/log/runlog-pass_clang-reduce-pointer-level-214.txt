===< 12588 >===
running 3 interestingness tests in parallel
===< pass_clang :: reduce-pointer-level >===
Termination check: size was 281; now 281
===================== done ====================

pass statistics:
  method pass_clang :: reduce-pointer-level worked 0 times and failed 1 times


          '/Users/ugurmeryem/Dropbox/sb/sample.c' reduced to:
#include <stdlib.h>
#include <stdio.h>

void foo() {
   int * p = (int *)malloc(10 * sizeof(int));
   int r = rand() % 10;
   if (r>10){
      free(p);
  	}
   free(p);
   printf("Done!\n");
}

void bar() {
   printf("Done!\n");
}

int main() {
   foo();
   //bar();
   return 0;
}