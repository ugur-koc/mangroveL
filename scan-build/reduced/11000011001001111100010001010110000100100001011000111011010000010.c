#include <stdlib.h>
#include <stdio.h>





void  main() {
   {
      int * p = (int *)malloc(10 * sizeof(int));
      int r = rand() % 10;
      if (r>10){
         free(p);
     	}
      free(p);
      0;
      
      (0);
   }
   
   ;
   //bar();
    0;
}